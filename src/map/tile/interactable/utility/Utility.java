package map.tile.interactable.utility;

import map.tile.interactable.Interactable;

public abstract class Utility extends Interactable {
    public Utility(int x, int y) {
        super(x,y);
    }
}
